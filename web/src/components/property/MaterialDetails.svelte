<script lang="ts">
  /**
   * MaterialDetails — inline material editor panel.
   * Currently material editing is handled by the React MaterialEditor modal.
   * This component is a placeholder for future inline editing within the PropertyPanel.
   * Fields: name, E, nu, rho, fy, preset selector, delete.
   */
</script>
